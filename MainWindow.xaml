<Window x:Class="XC_Studio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XC_Studio"
        mc:Ignorable="d"
        Title="XC Studio" Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        Icon="/icon/xc.ico"
        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
        MouseLeftButtonUp="TitleBar_MouseLeftButtonUp"
        MouseMove="Window_MouseMove">

    <Window.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 侧边栏按钮样式 -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFF1F1F1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3C3C3C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#FF1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <!-- 顶部标题栏 -->
            <RowDefinition Height="50"/>
            <!-- 菜单栏 -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
            <!-- 状态栏 -->
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="#FF2D2D30" 
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                MouseLeftButtonUp="TitleBar_MouseLeftButtonUp"
                MouseMove="Window_MouseMove">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 应用图标和标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                    <Image Source="/icon/xc.ico" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="XC Studio" FontSize="12" FontWeight="Medium" Foreground="White"/>
                </StackPanel>

                <!-- 窗口控制按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,5,0">
                    <Button Name="MinimizeButton" Background="Transparent" BorderThickness="0" 
                           Width="45" Height="30" Click="MinimizeButton_Click">
                        <TextBlock Text="━" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Name="MaximizeButton" Background="Transparent" BorderThickness="0" 
                           Width="45" Height="30" Click="MaximizeButton_Click">
                        <TextBlock Text="□" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                    </Button>
                    <Button Name="CloseButton" Background="Transparent" BorderThickness="0" 
                           Width="45" Height="30" Click="CloseButton_Click">
                        <TextBlock Text="✕" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFE81123"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 菜单栏 -->

        <!-- 主内容区域 - 应用集成工具布局 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="151"/>
                <ColumnDefinition Width="99"/>
                <!-- 左侧功能区 -->
                <ColumnDefinition Width="5"/>
                <!-- 分隔线 -->
                <ColumnDefinition Width="*"/>
                <!-- 主工作区 -->
            </Grid.ColumnDefinitions>

            <!-- 左侧功能区 -->
            <Border Grid.Column="0" Background="#FF252526" Grid.ColumnSpan="2" Margin="0,-47,0,0">
                <ScrollViewer PanningMode="VerticalOnly" VerticalScrollBarVisibility="Hidden" Margin="0,0,0,-10">
                    <StackPanel>
                        <!-- 快速访问 -->
                        <StackPanel>
                            <Button Style="{StaticResource SidebarButton}" Content="🏠 主页" 
                                       Click="HomeButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🔧 系统工具" 
                                       Click="SystemToolsButton_Click"/>
                        </StackPanel>

                        <!-- 应用程序 -->
                        <TextBlock Text="集成应用" FontSize="12" FontWeight="Bold" 
                                      Foreground="#FF007ACC" Margin="10,20,10,8"/>
                        <StackPanel>
                            <Button Style="{StaticResource SidebarButton}" Content="🌐 网络工具" 
                                       Click="NetworkToolsButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="📁 文件管理器" 
                                       Click="FileManagerButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🗄️ 数据库管理" 
                                       Click="DatabaseButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🔐 安全工具" 
                                       Click="SecurityButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🖼️ 图像处理" 
                                       Click="ImageProcessingButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="📝 文档编辑器" 
                                       Click="DocumentEditorButton_Click"/>
                        </StackPanel>

                        <!-- 开发工具 -->
                        <TextBlock Text="开发工具" FontSize="12" FontWeight="Bold" 
                                      Foreground="#FF007ACC" Margin="10,20,10,8"/>
                        <StackPanel>
                            <Button Style="{StaticResource SidebarButton}" Content="🎨 UI设计器" 
                                       Click="UIDesignerButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🧪 测试工具" 
                                       Click="TestToolsButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="📦 包管理器" 
                                       Click="PackageManagerButton_Click"/>
                        </StackPanel>

                        <!-- 系统设置 -->
                        <TextBlock Text="系统设置" FontSize="12" FontWeight="Bold" 
                                      Foreground="#FF007ACC" Margin="10,20,10,8"/>
                        <StackPanel>
                            <Button Style="{StaticResource SidebarButton}" Content="⚙️ 设置" 
                                       Click="SettingsButton_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="📋 关于" 
                                       Click="AboutButton_Click"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- 分隔线 -->
            <GridSplitter Grid.Column="2" Background="#FF464647" HorizontalAlignment="Stretch" Width="5" Margin="0,-47,0,0"/>

            <!-- 主工作区 -->
            <Grid Grid.Column="3" Background="#FF1E1E1E">
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <!-- 顶部功能区 -->
                    <RowDefinition Height="5"/>
                    <!-- 分隔线 -->
                    <RowDefinition Height="*"/>
                    <!-- 内容区域 -->
                </Grid.RowDefinitions>

                <!-- 顶部功能区 -->
                <Border Grid.Row="0" Background="#FF2D2D30" Padding="20" Margin="0,-48,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 当前页面标题 -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="/icon/xc.ico" Width="32" Height="32" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock x:Name="PageTitle" Text="XC Studio 主页" FontSize="20" FontWeight="Medium" 
                                              Foreground="White"/>
                                <TextBlock x:Name="PageDescription" Text="专业应用集成工具平台" FontSize="12" 
                                              Foreground="#FFCCCCCC"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 搜索框 -->
                        <Border Grid.Column="1" Background="#FF3C3C3C" CornerRadius="4" 
                                   Margin="50,0,20,0" VerticalAlignment="Center">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🔍" FontSize="14" Margin="10,0" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" 
                                           Foreground="White" Padding="0,5" FontSize="13"
                                           Text="搜索应用或工具..."/>
                            </Grid>
                        </Border>

                        <!-- 用户信息 -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- 分隔线 -->
                <Border Grid.Row="1" Background="#FF464647" Height="1"/>

                <!-- 内容区域 -->
                <Grid Grid.Row="2">
                    <!-- 主页内容 -->
                    <Grid x:Name="HomePageContent" Visibility="Visible">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- 快速启动卡片 -->
                        <StackPanel Grid.Row="0" Margin="30,20">
                            <TextBlock Text="快速启动" FontSize="16" FontWeight="Medium" 
                                          Foreground="White" Margin="0,0,0,15"/>
                            <WrapPanel Orientation="Horizontal">
                                <!-- 应用卡片 -->
                                <Border Background="#FF252526" CornerRadius="8" Width="180" Height="120" 
                                           Margin="0,0,20,20" Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="🌐" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="网络工具" FontSize="13" FontWeight="Medium" 
                                                      Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="网络诊断和监控" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#FF252526" CornerRadius="8" Width="180" Height="120" 
                                           Margin="0,0,20,20" Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="📁" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="文件管理器" FontSize="13" FontWeight="Medium" 
                                                      Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="高效文件操作" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#FF252526" CornerRadius="8" Width="180" Height="120" 
                                           Margin="0,0,20,20" Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="🗄️" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="数据库管理" FontSize="13" FontWeight="Medium" 
                                                      Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="数据库连接和查询" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#FF252526" CornerRadius="8" Width="180" Height="120" 
                                           Margin="0,0,20,20" Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="🔐" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="安全工具" FontSize="13" FontWeight="Medium" 
                                                      Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="系统安全扫描" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#FF252526" CornerRadius="8" Width="180" Height="120" 
                                           Margin="0,0,20,20" Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="🖼️" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="图像处理" FontSize="13" FontWeight="Medium" 
                                                      Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="图像编辑转换" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#FF252526" CornerRadius="8" Width="180" Height="120" 
                                           Margin="0,0,20,20" Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="📝" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="文档编辑器" FontSize="13" FontWeight="Medium" 
                                                      Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="文档处理编辑" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </StackPanel>

                        <!-- 系统信息面板 -->
                        <Border Grid.Row="1" Background="#FF252526" CornerRadius="8" 
                                   Margin="30,20,30,30" Padding="25">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="系统概览" FontSize="16" FontWeight="Medium" 
                                              Foreground="White" Margin="0,0,0,20"/>

                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- CPU 使用率 -->
                                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                            <TextBlock Text="CPU" FontSize="12" FontWeight="Medium" 
                                                      Foreground="#FF007ACC" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="CpuUsageText" Text="0%" FontSize="24" FontWeight="Bold" 
                                                      Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                            <TextBlock x:Name="CpuDetailsText" Text="使用率" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                        
                                        <!-- 内存使用 -->
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                            <TextBlock Text="内存" FontSize="12" FontWeight="Medium" 
                                                      Foreground="#FF007ACC" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="MemoryUsageText" Text="0GB" FontSize="24" FontWeight="Bold" 
                                                      Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                            <TextBlock x:Name="MemoryDetailsText" Text="已使用" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                        
                                        <!-- 磁盘使用 -->
                                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                            <TextBlock Text="磁盘" FontSize="12" FontWeight="Medium" 
                                                      Foreground="#FF007ACC" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="DiskUsageText" Text="0GB" FontSize="24" FontWeight="Bold" 
                                                      Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                            <TextBlock x:Name="DiskDetailsText" Text="可用空间" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                        
                                        <!-- 网络状态 -->
                                        <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                                            <TextBlock Text="网络" FontSize="12" FontWeight="Medium" 
                                                      Foreground="#FF007ACC" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="NetworkStatusText" Text="检查中" FontSize="24" FontWeight="Bold" 
                                                      Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                            <TextBlock x:Name="NetworkDetailsText" Text="连接状态" FontSize="10" 
                                                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- 其他页面内容（默认隐藏） -->
                    <Grid x:Name="OtherPageContent" Visibility="Collapsed">
                        <TextBlock Text="功能页面加载中..." FontSize="18" Foreground="White" 
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="#FF007ACC">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 状态信息 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                    <TextBlock Text="就绪" Foreground="White" FontSize="12" Margin="0,0,15,0"/>
                    <TextBlock Text="行 1, 列 1" Foreground="#FFCCCCCC" FontSize="11" Margin="0,0,15,0"/>
                    <TextBlock Text="UTF-8" Foreground="#FFCCCCCC" FontSize="11"/>
                </StackPanel>

                <!-- 右侧信息 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                    <TextBlock Text="软件发行方为谢晨工作室" Foreground="White" FontSize="11" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock Text="最终解释权为工作室所属" Foreground="#FFCCCCCC" FontSize="11"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
