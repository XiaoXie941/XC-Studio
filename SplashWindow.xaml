<Window x:Class="XC_Studio.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XC_Studio"
        mc:Ignorable="d"
        Height="450" Width="800"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        ShowActivated="False">

    <Window.Resources>
        <!-- 动画资源 -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5"/>
        </Storyboard>

        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <!-- 背景渐变效果 -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF2D2D30" Offset="0"/>
                <GradientStop Color="#FF1E1E1E" Offset="0.5"/>
                <GradientStop Color="#FF007ACC" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 主内容区域 -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,23,0,84">
            <!-- Logo和标题 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20" Height="101">
                <Image Source="/icon/xc.ico" Width="64" Height="64" Margin="0,0,20,0"/>
                <TextBlock Text="XC Studio" FontSize="42" FontWeight="Light" 
                          Foreground="White" VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Color="Black" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>

            <!-- 版本信息 -->
            <TextBlock FontSize="14" 
                      Foreground="#FFCCCCCC" HorizontalAlignment="Center"><Run Text="V1.0.0"/><Run Language="zh-cn" Text=" "/><Run Text="公开版"/></TextBlock>
        </StackPanel>

        <!-- 加载进度区域 -->
        <StackPanel Grid.Row="2" Margin="80,0,80,20">
            <!-- 进度条 -->
            <Border Background="#FF3C3C3C" Height="4" CornerRadius="2" ClipToBounds="True">
                <Rectangle x:Name="ProgressBar" Fill="#FF007ACC" Height="4" 
                          HorizontalAlignment="Left" Width="0"/>
            </Border>

            <!-- 状态文本 -->
            <TextBlock x:Name="StatusText" Text="正在启动..." FontSize="12" 
                      Foreground="#FFCCCCCC" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- 版权信息 -->
        <TextBlock Grid.Row="3" 
                  FontSize="11" Foreground="#FF999999" HorizontalAlignment="Center" 
                  Margin="0,0,0,20"><Run Text="© 202"/><Run Language="zh-cn" Text="5"/><Run Text=" XC Studio. 谢晨工作室发行."/></TextBlock>
    </Grid>
</Window>