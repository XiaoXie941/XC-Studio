<Window x:Class="XC_Studio.NetworkToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XC_Studio"
        mc:Ignorable="d"
        Title="网络工具 - XC Studio" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
        MouseLeftButtonUp="TitleBar_MouseLeftButtonUp"
        MouseMove="Window_MouseMove">

    <Window.Resources>
        <!-- 引用主窗口的按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- 顶部标题栏 -->
            <RowDefinition Height="*"/>
            <!-- 主内容区 -->
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="#FF2D2D30" Padding="20,15" Height="67" VerticalAlignment="Top" Grid.RowSpan="2"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                MouseLeftButtonUp="TitleBar_MouseLeftButtonUp"
                MouseMove="Window_MouseMove">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🌐" FontSize="24" Margin="0,0,15,0"/>
                    <StackPanel>
                        <TextBlock Text="网络工具" FontSize="20" FontWeight="Medium" Foreground="White" Height="23"/>
                        <TextBlock Text="网络连接诊断、端口扫描和网络监控" FontSize="12" Foreground="#FFCCCCCC" Height="14" RenderTransformOrigin="0.641,0.41"/>
                    </StackPanel>
                </StackPanel>

                <!-- 关闭按钮 -->
                <Button Grid.Column="2" x:Name="CloseButton" Background="Transparent" BorderThickness="0" 
                       Width="45" Height="30" Click="CloseButton_Click">
                    <TextBlock Text="✕" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFE81123"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 网络连接测试区域 -->
            <Border Grid.Row="0" Background="#FF252526" CornerRadius="8" Margin="30,20,30,10" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="网络连接测试" FontSize="16" FontWeight="Medium" 
                              Foreground="White" Margin="0,0,0,15"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#FF3C3C3C" CornerRadius="4" Margin="0,0,10,0" 
                               VerticalAlignment="Center" Padding="10,5">
                            <TextBox x:Name="NetworkTestUrl" Background="Transparent" BorderThickness="0" 
                                   Foreground="White" FontSize="14" Text="https://www.bilibili.com"/>
                        </Border>

                        <Button Grid.Column="1" x:Name="TestConnectionButton" Style="{StaticResource ModernButton}" 
                               Content="测试连接" Click="TestConnectionButton_Click"/>
                    </Grid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
                        <Border Background="#FF1E1E1E" CornerRadius="4" Padding="15,10" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="状态:" Foreground="#FFCCCCCC" FontSize="12" Margin="0,0,5,0"/>
                                <TextBlock x:Name="ConnectionStatusText" Text="未测试" Foreground="White" FontWeight="Bold" FontSize="12"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#FF1E1E1E" CornerRadius="4" Padding="15,10" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="响应时间:" Foreground="#FFCCCCCC" FontSize="12" Margin="0,0,5,0"/>
                                <TextBlock x:Name="ConnectionTimeText" Text="--" Foreground="White" FontWeight="Bold" FontSize="12"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#FF1E1E1E" CornerRadius="4" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="IP地址:" Foreground="#FFCCCCCC" FontSize="12" Margin="0,0,5,0"/>
                                <TextBlock x:Name="IpAddressText" Text="--" Foreground="White" FontWeight="Bold" FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 网络信息区域 -->
            <Border Grid.Row="1" Background="#FF252526" CornerRadius="8" Margin="30,10,30,30" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="网络接口信息" FontSize="16" FontWeight="Medium" 
                              Foreground="White" Margin="0,0,0,15"/>

                    <ListView x:Name="NetworkInterfacesList" Grid.Row="1" Background="#FF1E1E1E" 
                             BorderThickness="1" BorderBrush="#FF464647" Foreground="White" FontSize="12">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="200"/>
                                <GridViewColumn Header="类型" DisplayMemberBinding="{Binding Type}" Width="120"/>
                                <GridViewColumn Header="状态" DisplayMemberBinding="{Binding Status}" Width="100"/>
                                <GridViewColumn Header="速度" DisplayMemberBinding="{Binding Speed}" Width="120"/>
                                <GridViewColumn Header="IP地址" DisplayMemberBinding="{Binding IpAddress}" Width="150"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>